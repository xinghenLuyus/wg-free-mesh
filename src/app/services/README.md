# services

`services/` 保存应用服务和用例编排。

## 当前状态

第一版骨架暂未创建具体服务，API 通过内存仓储完成联调。

## 后续方向

- `ConfigService`
- `NodeService`
- `MeshService`
- `WireGuardConfigService`
- `EndpointControlService`
- `BackupService`

