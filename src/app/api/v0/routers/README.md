# api/v0/routers

`api/v0/routers/` 保存开发测试专用路由文件。

## 当前文件

- `dev.py`
  - `reset_bootstrap()`：清空管理员密码、token secret、界面语言和界面主题偏好，让系统重新进入 setup 流程。

## 约定

- 这里只放开发和联调用接口，不放正式业务接口。
- 该目录下的接口默认不要求后台登录 token，便于命令行直接调用。
- 任何新增接口都必须明确写清“会清什么、不清什么”，避免误删业务数据。
