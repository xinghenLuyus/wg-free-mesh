# Snapshots

Snapshots are encrypted application-level exports, not raw database backups.

They include application data such as configs, nodes, peer links, sync/runtime state, client credentials, MCP tokens, audit logs, and settings.

The administrator password is not included.

Snapshot restore is only available in the system UI. MCP can only export an existing snapshot through a short-lived download URL.
