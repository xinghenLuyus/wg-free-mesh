# 迁移计划

## 阶段 0：文档确认

已完成初版文档确认，当前进入前后端骨架落地阶段。

目标：

- 确认根目录结构。
- 确认前后端职责。
- 确认后端模块划分。
- 确认前端技术栈。
- 确认安全边界。

产物：

- `docs/`
- 各顶层目录 README。

## 阶段 1：后端骨架

已完成第一版后端骨架。

内容：

- 初始化 `src/pyproject.toml`。
- 创建 FastAPI app。
- 创建配置、日志、异常、响应 envelope。
- 创建 `/api/v1/system/health`。
- 建立测试框架。
- Alembic 目录将在数据库模型确定后创建。

验收：

- 后端可启动。
- health API 可访问。
- 测试可运行。

## 阶段 2：领域模型和数据库

当前已完成 SQLite 持久化仓储，下一步进入正式领域模型和迁移体系。

内容：

- 设计 ORM 模型。
- 设计 Pydantic Schema。
- 建立 repository 层。
- 建立 migration。

优先领域：

- Auth
- Config
- Node
- Mesh Link
- Settings

## 阶段 3：核心业务服务

内容：

- 配置生成。
- 节点校验。
- Peer 链路校验。
- 状态计算。
- 配置应用。

目标：

- 把旧前端中的核心业务逻辑迁移到后端。

## 阶段 4：前端骨架

已完成第一版前端骨架。

内容：

- 初始化 Vue 3 + Vite + TypeScript。
- 接入 Element Plus。
- 接入 Router、Pinia、API client。
- 创建基础布局和登录页。

## 阶段 5：功能页迁移

建议顺序：

1. 登录和初始化。
2. 配置列表。
3. 配置详情和节点管理。
4. Mesh 链路。
5. 配置应用。
6. 端点控制。
7. 设置。
8. 备份恢复。

## 阶段 6：客户端再设计

当前不处理 `client/`。

后续单独确认：

- 是否继续 Go Agent。
- 是否保留 MQTT。
- 是否需要多 profile。
- 是否需要跨平台。
