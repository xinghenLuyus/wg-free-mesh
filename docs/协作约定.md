# 协作约定

## 当前阶段约定

- 旧代码已经归档到 `bak/`，新架构不直接复用旧目录。
- 未经确认，不从 `bak/` 复制代码到新目录。
- 当前已进入代码骨架落地和可运行验证阶段。

## 文档同步规则

所有变更都必须同步更新文档。

必须更新文档的情况：

- 新增目录。
- 删除目录。
- 修改 API。
- 修改数据结构。
- 修改前后端职责。
- 修改安全策略。
- 修改 Docker 部署。
- 修改客户端协议。
- 迁移旧逻辑。
- 调整可被外部 API 或 MCP 复用的业务规则。

## 开发边界

- `src/` 只放后端。
- `front/` 只放前端。
- `client/` 当前保持空。
- `docker/` 只放容器和环境配置。
- `docs/` 只放文档。
- `bak/` 只读参考，不参与运行。
- `bak/` 已被 `.gitignore` 屏蔽，不参与版本管理。

## 工具与权限约定

- 不允许因为本地权限问题在项目中引入与业务无关的临时目录、缓存目录或替代性产物。
- 遇到依赖安装、构建、缓存目录、端口、进程、全局 store 等权限问题时，优先直接提权执行正确命令，不通过污染项目目录来绕过。
- 禁止通过 `--store-dir` 把 pnpm store 临时落到项目根目录或工作区内；如需安装依赖，先确认使用既有全局 store 或提权执行标准命令。
- 如果已经产生了无关目录，必须第一时间删除，并在文档中记录原因和处理方式。
- 像 `bak/`、依赖目录、运行数据目录之外的临时目录，默认视为不应进入项目结构。

## 业务下沉约定

- 后端是业务真相来源，前端只负责展示、输入、交互和调用。
- WireGuard 配置生成、Mesh 连接草稿、Endpoint 自动摘要、标签批量一致性、配置三态、同步状态等逻辑必须在后端实现。
- 后续外部 API 和 MCP 聚合器需要复用的能力，必须先抽象为后端用例，再由前端调用。
- 前端不得为了快速实现页面，在浏览器里复制一套后端业务规则。

## 国际化约定

- 新增前端用户可见文案必须写入 `front/src/i18n/messages/zh-CN.ts` 和 `front/src/i18n/messages/en-US.ts`。
- 页面组件、组合式函数、通知工具和表单规则不得直接写死用户可见中文或英文业务文案。
- 新增后端错误码必须同步补充前端 `errors` 翻译。
- 后端默认错误消息使用英文，前端根据错误码和当前语言负责最终展示。
- 后端不得为网页展示拼接多语言句子，也不得要求前端依赖后端中文 `message`。

## Git 约定

- 旧历史已清空，新仓库从当前重构版本重新开始。
- `bak/` 仅作为旧代码参考，禁止纳入版本管理。
- `node_modules/`、构建产物、运行数据、缓存目录禁止纳入版本管理。
- 提交前必须确认 `git status --short --ignored` 中只有预期的 ignored 项。

## 审核流程

1. 先提交架构文档。
2. 用户审核并确认。
3. 再进行目录骨架或代码生成。
4. 每完成一个阶段，更新对应文档。
