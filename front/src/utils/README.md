# utils

`utils/` 保存前端纯工具函数。

## 约定

- 全局反馈统一使用 `notify.ts`。
- `notify` 默认固定右上角弹窗展示。
- 通知关闭按钮外框展示环形进度，进度结束后自动关闭。
- 鼠标悬停通知时暂停进度和自动关闭计时。
- 页面代码不直接使用 `ElMessage` 做业务反馈，避免不同页面提示样式分裂。
- `nodePayload.ts` 负责把节点读模型转换为更新请求，避免编辑标签或端点设置时丢字段。
