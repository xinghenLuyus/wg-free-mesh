# AGENT.md

本文件约束 Codex / 自动化编码助手在本仓库内的行为。所有自动化协作必须同时遵守 [docs/协作约定.md](D:/wenjian/stepsave/project/wg-free-mesh/docs/协作约定.md)。

## 基本原则

- 先读文档和现有代码，再判断实现方式。
- 变更必须贴合当前架构，不引入与项目方向冲突的临时方案。
- 后端是业务真相来源，前端只负责展示、输入、交互和调用。
- 所有会影响外部行为、目录结构、API、协议、Docker、数据结构、安全策略或职责边界的变更，都必须同步更新文档。

## 禁止擅自执行的操作

除非用户明确授权，不得主动执行以下操作：

- 启动后端、前端、Docker、EMQX、数据库或客户端 agent。
- 执行构建命令，包括但不限于 `pnpm run build`、`go build`、Docker build、客户端发布构建。
- 执行会修改运行数据、启动服务、拉起容器、安装系统服务或改变本机环境的命令。
- 删除、移动或重置用户未明确要求处理的文件。

默认允许的验证范围：

- 类型检查，例如 `pnpm run typecheck`、`python -m mypy app`。
- 不启动服务、不构建产物、不写入业务运行数据的静态检查。
- 用户明确要求时，可执行对应测试；如果测试会生成缓存，必须确保缓存位置符合本文件约束。

## 工作区污染约束

- 不允许因为权限、缓存、依赖安装、构建或工具限制，在项目中引入与业务无关的临时目录、缓存目录或替代性产物。
- 遇到权限不足、缓存目录不可写、全局 store 不可写、端口或系统服务权限受限等问题时，必须第一时间申请提权并说明用途，不得通过改路径、改缓存位置、落入工作区或降低命令语义来绕过。
- 禁止把 pnpm store、Go cache、临时下载目录、工具缓存等落入项目根目录或源码目录。
- 禁止使用 `--store-dir`、临时环境变量或自定义路径把外部工具缓存绕到工作区内，除非该目录已被项目明确设计、文档化并纳入忽略规则。
- 如果必须使用缓存目录，优先使用系统默认缓存或用户明确批准的位置。
- 如果已经产生无关目录或文件，必须第一时间说明来源，并在获得确认后清理。

## 目录边界

- `src/` 只放后端代码、后端测试、后端迁移和后端文档。
- `front/` 只放前端代码、前端资源和前端文档。
- `client/` 只放 Go 客户端代码、客户端构建脚本和客户端文档。
- `docker/` 只放容器、Compose、环境示例和 Docker 相关文档。
- `docs/` 只放项目级设计、协议、架构和协作文档。
- 根目录只放项目级入口文件和明确需要全局可见的配置文件。

## 文档协作

- 代码变更完成前，检查是否需要同步 `docs/`、`src/README.md`、`front/README.md`、`client/README.md` 或 `docker/README.md`。
- 不在文档中保留过时的阶段性语境、旧架构命名或已经删除的目录描述。
- 不把临时调试结论写成正式能力说明。
- 文档应描述当前事实，不使用“未来版本”“v1/v2”等开发阶段遗留说法。

## 前端约束

- 前端不得复制后端业务规则，不得自行推导在线状态、配置同步态、产物缓存状态等服务端事实。
- 新增用户可见文案必须同步写入中英文 i18n 文件。
- 页面代码不要直接使用 `ElMessage`，确认类交互仍可使用 `ElMessageBox`。

## 后端约束

- 新业务规则优先进入服务层或仓储层，不把可复用逻辑散落在路由中。
- API、数据结构、错误码、后台任务、Docker 环境变量变化必须同步文档。
- 数据库变更必须纳入迁移管理，不能只靠运行时隐式补表补字段。

## 客户端约束

- 客户端命令语义必须与文档一致。
- 正式客户端版本只从统一版本来源读取，不在客户端源码中维护第二份版本号。
- 手动构建仅用于用户明确授权的调试；默认不主动构建客户端二进制。

## Git 与清理

- 不回滚用户改动，除非用户明确要求。
- 不提交、暂存或修改与当前任务无关的文件。
- `node_modules/`、构建产物、运行数据、缓存目录不得进入版本管理。
- 完成变更后，应说明修改范围和已执行的验证；未执行构建或运行时，也要明确说明。
